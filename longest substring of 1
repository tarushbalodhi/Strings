int maxlength( string s)
{
    int maxlen = 0;
        int len = 0;
        // int i = 0 , j = 1;
    for(char z : s){
        if(z == '1'){
            len++;
            maxlen = max(maxlen, len);
        }
        else len = 0;
    }
    return maxlen;
}
